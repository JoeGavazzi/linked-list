package com.subqstudios;

public class LinkedListTest {

    // TODO: write test for:

    // E getFirst()
    // E getLast()
    // void addFirst(E e)
    // void addLast(E e)
    // boolean add(E e)

    // Node<E> addBefore(E e, Node<E> n)
        // correct functionality
        // add before head updates tail pointer
        // add before tail updates head pointer


    // Node<E> getNode(int index)
        // throws IndexOutOfBounds index < 0 || >= size
        // gets correct node
        // starts at tail to get node near end
        // starts at head to get node near front

    // E removeNode(Node<E> n)

    // listIterator(int index)

}
